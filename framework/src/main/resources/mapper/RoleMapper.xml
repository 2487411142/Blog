<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.limemartini.mapper.RoleMapper">
    <select id="selectRoleKeysByUserId" resultType="java.lang.String">
        select DISTINCT role_key
        from blog.sys_user_role ur
                 left join blog.sys_role role on ur.role_id = role.id
        where role.status = '0'
          and role.del_flag = '0'
          and ur.user_id = #{userId};
    </select>
</mapper>